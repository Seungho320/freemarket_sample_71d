= render "fixed-bar/header"
.signUp_main
  .signUp_main__form
    .signUp_main__form__header
      住所情報登録
    .signUp_main__form__contents
      = form_for @address do |f|
        = render "devise/shared/error_messages", resource: @address
        .field
          = f.label :名字
          %em 必須
          %br/
          = f.text_field :first_name, autofocus: true
        .field
          = f.label :名前
          %em 必須
          %br/
          = f.text_field :last_name, autofocus: true
        .field
          = f.label :名字（フリガナ）
          %em 必須
          %br/
          = f.text_field :first_name_kana, autofocus: true
        .field
          = f.label :名前（フリガナ）
          %em 必須
          %br/
          = f.text_field :last_name_kana, autofocus: true
        .field
          = f.label :郵便番号
          %em 必須
          %br/
          = f.text_field :zipcode, autofocus: true
        .field
          = f.label :都道府県
          %em 必須
          %br/
          = f.collection_select :area_id, Area.all, :id, :name, autofocus: true, prompt: "選択してください"
        .field
          = f.label :市区町村
          %em 必須
          %br/
          = f.text_field :city, autofocus: true
        .field
          = f.label :番地
          %em 必須
          %br/
          = f.text_field :address, autofocus: true
        .field
          = f.label :建物名（任意）
          %br/
          = f.text_field :bulding
        .actions
          = f.submit "次へ"
= render "fixed-bar/footer"
-# = render "devise/shared/links"
